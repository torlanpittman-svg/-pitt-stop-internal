# Pitt Stop OS — Session Handoff (2026-07-30)

Paused cleanly at owner's request. Working tree clean, all commits pushed to `origin/main`, nothing in a partial state. No new implementation, no deploys, no production QuickBooks writes pending.

---

## Current Status

**Completed this session — Vercel Blob image persistence brought fully online for Dealer Check-In v2.**

- Diagnosed and resolved the Blob upload failure. Two root causes, in order:
  1. The originally linked Blob store was **private access**, but `platform/blob.ts` uploads with `access: 'public'` → *"Cannot use public access on a private store."*
  2. After the owner switched to a **public** store, that minted a **new `BLOB_READ_WRITE_TOKEN`**; the running build still had the old token → *"This store does not exist."* Fixed with a fresh **git** deploy (note: `vercel redeploy` alone does **not** refresh env vars).
- Removed the temporary `GET /api/dealer-checkin/blob-check` diagnostic endpoint (now returns 404).
- Updated project memory (`project-pitt-stop-os.md`) with the Blob-linked/verified state and the public-store rule.

**Deployed this session:**
- `a0670ad` — empty commit to force a fresh build picking up the new (public-store) token.
- `2a7dc47` — removed the temporary blob-check diagnostic. **This is the current production build.**
- Production URL: `https://pitt-stop-internal.vercel.app` — root returns `200`.

**Verified this session (all green):**
- Public blob upload succeeds; token present.
- Uploaded object is **publicly accessible**: `GET → 200` at `7ac7k9ngmmv5hiok.public.blob.vercel-storage.com`.
- Real OCR pipeline (`POST /api/dealer-checkin/ocr`) returns a public `photoUrl` that resolves `200`.
- Save → retrieve → Scan History path: a controlled test scan persisted `photo_url` + `raw_ocr` + an `edited` audit event and appeared in `listRecentScans`. **Test row was deleted afterward — production data is clean.**
- `blob-check` diagnostic confirmed removed (`404`).

**Production changes made this session:**
- Code deploys only (the two commits above). **No production QuickBooks writes. No production data changes** (the single verification row was inserted and then deleted). The validated QB write / approval / audit path was not touched.

**Current git state:** branch `main`, HEAD = `2a7dc47`, clean tree, fully pushed.

---

## Production Status

- **Dealer Check-In workflow:** Live and production-validated. Entry = Take Photo / Upload Photo → clean editable review (Dealer, Stock #, Year, Make, Model, Color, Service, Price; no confidence scores; stock always editable and re-resolves the dealer on prefix change; "use anyway" override) → **Confirm and Send to QuickBooks**. First real end-to-end check-in already succeeded earlier (invoice #100809). Employee-confirmed values are the source of truth.
- **QuickBooks integration:** Production connected. Company **"Pitt Stop Detail & Auto Sales"**, realm **123146329198289**, environment=production, status active. Production write path guarded by `X-QB-Write-Approved: true` (confirm button). DocNumber is explicitly assigned on create (company has "Custom transaction numbers" enabled). Live Sterling customer IDs: Auto Group **6** (S,T), Kia **67** (K), Subaru **836** (U). Company invoice sequence ≈ 100809, next created ≈ 100810.
- **Image persistence:** **Working.** Original tag image stored in Vercel Blob via `platform/blob.ts` `uploadPhoto` on every scan through the OCR route; deduped by sha-256 `image_hash`; returns a public `photoUrl`. Degrades gracefully if Blob is ever unset.
- **Audit logging:** Intact and unchanged. `dealer_scans` + `dealer_scan_events` record raw OCR, OCR→confirmed `edited` diffs, confirmed values, `approved_by`, and QB txn id. Architecture explicitly not modified.
- **Blob storage:** Public store linked and verified. Host `7ac7k9ngmmv5hiok.public.blob.vercel-storage.com`. `BLOB_READ_WRITE_TOKEN` present in Production + Preview. **Rule: the store must stay PUBLIC** — both the estimator and dealer images display via direct `<img src>`.
- **Scan History (admin):** `/admin/dealer-checkin/history` — thumbnails, vehicle, dealer, invoice #, status; tap image for full size; "Mark reviewed (delete image)" purges one now. Behind `proxy.ts` basic-auth.
- **Retention:** `IMAGE_RETENTION_DAYS` (default 90); daily cron `/api/cron/drain-dealer-queue` also runs `cleanupDealerImages()`.

**Known limitations:**
- No real end-to-end check-in has yet been run *with image persistence active* (the plumbing is verified; the owner's phone test is the final confirmation).
- Switching the Blob store to private again would break image display (would require signed-URL rework touching the shared module + estimator).
- PWA: owner still needs to delete + re-add the Home Screen icon once to clear the old cache / register the service worker.
- No per-user auth; any operator can override validation via explicit confirm (by design).

---

## Outstanding Work (priority order)

1. **Owner runs one controlled real end-to-end Dealer Check-In on their phone** (with image persistence now live).
2. **Read-only verification after that test:** QB invoice/appended line correct; original image saved; scan record has raw OCR + confirmed values; edits recorded; image shows in `/admin/dealer-checkin/history` associated with the correct scan + invoice.
3. Continue the controlled rollout — one real vehicle at a time (preview → Confirm Production Write → verify).
4. PWA: owner deletes + re-adds the Home Screen icon once.
5. Deferred polish: 180×180 PNG app icon (currently SVG — cosmetic).

---

## Next Recommended Task

**Support the owner's one controlled end-to-end Dealer Check-In test, then read-only verify it.** No code needed first — the plumbing is verified and production is ready. When the owner completes the scan, trace it end-to-end read-only: `node scripts/trace-scan.mjs`, `GET /api/quickbooks/query-invoice?id=|customerId=`, and confirm the image + audit trail in `/admin/dealer-checkin/history`. Only then continue the controlled rollout.

---

## Blockers (need owner action / approval)

- **Owner must perform the real end-to-end check-in** — production QB writes require an operator tapping Confirm on a real vehicle; I will not fabricate lines on real dealer invoices.
- **Keep the Vercel Blob store PUBLIC** — do not switch it back to private.
- **PWA Home Screen** re-add is a manual owner step.

---

## Resume Instructions

Send this next time to resume with full context:

> Resume Pitt Stop OS from the 2026-07-30 handoff (`docs/HANDOFF_2026-07-30.md`). Blob image persistence is linked and verified end-to-end; production build is commit `2a7dc47`; no work is in progress. I have now run [or: am about to run] one real Dealer Check-In through the phone app. Verify it read-only: confirm the QuickBooks invoice/appended line is correct, the original tag image was saved to Blob, the scan record holds raw OCR + confirmed values, any edits were recorded, and the image appears in `/admin/dealer-checkin/history` associated with the correct scan and invoice. Do not make unrelated changes and do not perform any production QuickBooks writes yourself.
